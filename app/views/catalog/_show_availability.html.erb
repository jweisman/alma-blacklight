<div>
    <p><button id="physical-<%= document.id %>" class="btn btn-default availability" data-url="<%= "#{openurl(document.id,'getit')}" %>"><i class="fa fa-chevron-right"></i> Physical copy</button>
    <button id="online-<%= document.id %>" class="btn btn-default availability" data-url="<%= "#{openurl(document.id,'viewit')}" %>"><i class="fa fa-chevron-right"></i> On-line version</button>
    </p>  
    <div class="fulfillment-dt" style="display: none; position: relative;" width=100% height=300>
        <%= content_tag :table, class: "table responsive", role: :datatable, data: { url: "/almaws/bibs/#{document.id}", id: document.id} do %>
    		<thead>
	    		<tr>
	    			<th></th>
                    <th></th>
	    			<th></th>
                    <th></th>
	    		</tr>
    		</thead>
    		<tbody>
    		</tbody>
    	<% end %>
    </div> 
</div>